"""Налаштування агента. Значення можна перевизначити через змінні середовища."""
import os
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent


def _load_dotenv() -> None:
    """Мінімальний завантажувач .env — без зовнішніх залежностей."""
    env_file = ROOT / ".env"
    if not env_file.exists():
        return
    for line in env_file.read_text(encoding="utf-8-sig").splitlines():
        line = line.strip()
        if not line or line.startswith("#") or "=" not in line:
            continue
        key, _, value = line.partition("=")
        os.environ.setdefault(key.strip(), value.strip().strip('"').strip("'"))


_load_dotenv()

# --- Обов'язкові секрети ---
TELEGRAM_BOT_TOKEN = os.environ.get("TELEGRAM_BOT_TOKEN", "")
TELEGRAM_CHANNEL = os.environ.get("TELEGRAM_CHANNEL", "")
GEMINI_API_KEY = os.environ.get("GEMINI_API_KEY", "")
# Особистий chat_id власника: сюди бот шле сповіщення про збої (необов'язково)
TELEGRAM_ADMIN_CHAT = os.environ.get("TELEGRAM_ADMIN_CHAT", "")

GEMINI_MODEL = os.environ.get("GEMINI_MODEL", "gemini-2.5-flash")
# Резервна модель: у кожної моделі своя квота безкоштовного тарифу.
# Аудит 15.08.2026: попереднє значення "gemini-2.0-flash" Google прибрав, і
# воно віддавало 404 — тобто резерв був мертвий, а в логах це губилося серед
# звичайних 429. Тому тепер АЛІАС, а не конкретна версія: він переїжджає на
# актуальну модель сам і не помре мовчки при наступному виведенні з ужитку.
# (Перевірено: "gemini-2.5-flash-lite" числиться в списку моделей, але теж
# віддає 404 — покладатися на список не можна, лише на реальний запит.)
GEMINI_FALLBACK_MODEL = os.environ.get("GEMINI_FALLBACK_MODEL", "gemini-flash-lite-latest")
# Другий AI-провайдер (безкоштовний Groq): вмикається, коли Gemini без квоти
GROQ_API_KEY = os.environ.get("GROQ_API_KEY", "")
GROQ_MODEL = os.environ.get("GROQ_MODEL", "openai/gpt-oss-120b")
# Третій і четвертий AI-провайдер (Cloudflare Workers AI, OpenRouter):
# додано 2026-08-02, коли GitHub Models пішов у retirement brownout (410
# Gone) і весь каскад одночасно впав разом з вичерпаною квотою Gemini/Groq —
# канал на 30+ хв лишився без постів із TG-трендів (Ukrnet-фолбек без AI все
# ще працював). Cerebras пробували першим, але його "безкоштовний" $5-кредит
# насправді вимагає прив'язки картки — відмовились.
#
# Cloudflare Workers AI — найщедріший реально безкоштовний (без картки)
# варіант: 10 000 "нейронів"/добу (≈1300 відповідей LLM), forever-free,
# дозволений для production. Потребує ДВА значення (не один ключ) —
# Account ID + API Token. JSON-режим у моделей Workers AI задокументований
# непослідовно, тож _cloudflare_json парсить лениво (_lenient_json).
CLOUDFLARE_ACCOUNT_ID = os.environ.get("CLOUDFLARE_ACCOUNT_ID", "")
CLOUDFLARE_API_TOKEN = os.environ.get("CLOUDFLARE_API_TOKEN", "")
CLOUDFLARE_MODEL = os.environ.get("CLOUDFLARE_MODEL", "@cf/meta/llama-3.3-70b-instruct-fp8-fast")
# ВІДОМА ОБМЕЖЕНІСТЬ: Workers AI має default max_tokens=256, і компат-шар,
# схоже, ігнорує наше явне max_tokens=4000 — короткі JSON (перевірка
# дублів) проходять, повний пост (заголовок+абзаци) обривається на
# середині. Лишили свідомо (User: "лишити як є") — каскад безпечно падає
# далі, шкоди нема, а для коротких перевірок все ж корисний.
#
# xAI Grok розглядали як п'ятий провайдер (обіцяли $175/міс безкоштовних
# кредитів через opt-in "data sharing"), але на практиці для нашого
# акаунта такого варіанту в консолі не знайшлось — лише платний
# Pay-as-you-go з карткою. Не додавали.
# OpenRouter — без картки, але скромний ліміт (50 запитів/добу, 20/хв),
# лишається додатковим шансом після Cloudflare. МОДЕЛЬ РОТУЄТЬСЯ: список
# безкоштовних (":free") моделей на openrouter.ai змінюється щотижня —
# якщо OPENROUTER_MODEL за замовчуванням почне повертати 404/зникне зі
# списку, звірити актуальну назву на https://openrouter.ai/models?max_price=0
# і перевизначити змінною середовища/секретом OPENROUTER_MODEL. ВАЖЛИВО: не
# кожна ":free" модель підтримує response_format json_object (наш
# обов'язковий контракт) — реальний кейс: перший обраний варіант
# (inclusionai/ling-3.0-flash) повертав 400 Bad Request саме через це.
# Перевіряти supported_parameters моделі на openrouter.ai/api/v1/models
# перед заміною.
OPENROUTER_API_KEY = os.environ.get("OPENROUTER_API_KEY", "")
OPENROUTER_MODEL = os.environ.get("OPENROUTER_MODEL", "google/gemma-4-31b-it:free")
# П'ятий AI-провайдер — GitHub Models. У GitHub Actions працює через вбудований
# GITHUB_TOKEN (потрібен permissions: models: read у workflow), без окремого ключа.
# Лишений останнім у каскаді (не видалений) — на випадок, якщо retirement
# brownout виявиться тимчасовим, а не остаточним згортанням сервісу.
GITHUB_TOKEN = os.environ.get("GITHUB_TOKEN", "") or os.environ.get("GH_MODELS_TOKEN", "")
GITHUB_MODEL = os.environ.get("GITHUB_MODEL", "openai/gpt-4o-mini")
# Той самий довгоживучий токен, що продовжує crosspost.py — тільки для читання
# (stats.py викликає лише *_insights, нічого не публікує й не продовжує сам).
THREADS_TOKEN = os.environ.get("THREADS_TOKEN", "")

# Чи доступний хоч один AI-провайдер (для генерації текстів)
AI_AVAILABLE = bool(
    GEMINI_API_KEY or GROQ_API_KEY
    or (CLOUDFLARE_ACCOUNT_ID and CLOUDFLARE_API_TOKEN)
    or OPENROUTER_API_KEY or GITHUB_TOKEN
)

# Таймаути AI-запитів, секунд. Було 60 на КОЖНОГО провайдера — при повному
# каскаді (Gemini ×2 → Groq → Cloudflare → OpenRouter → GitHub) один JSON-виклик
# міг з'їсти до 6 хв, а їх кілька за запуск. Реальний наслідок 06.08.2026:
# job "post" тривала 9-22 хв при тригерах кожні 5 хв, черга скасовувала
# наступні запуски, канал мовчав по 36-79 хв. Первинному провайдеру лишаємо
# більше часу (він реально генерує текст), фолбекам — рівно стільки, щоб
# встигла нормальна відповідь, але зависання не коштувало хвилин.
AI_TIMEOUT_PRIMARY = int(os.environ.get("AI_TIMEOUT_PRIMARY", "45"))
AI_TIMEOUT_FALLBACK = int(os.environ.get("AI_TIMEOUT_FALLBACK", "30"))
# GitHub Models у retirement brownout — від нього чекати нічого, хай падає швидко
AI_TIMEOUT_GITHUB = int(os.environ.get("AI_TIMEOUT_GITHUB", "15"))

# --- Джерело новин ---
FEED_URL = "https://www.ukr.net/news/main.html"
# Шлюзи-читалки для обходу блокування дата-центрових IP (GitHub Actions).
# Укрнет віддає стрічку прямо лише «житловим» IP; з раннера — 403, тому
# читаємо через проксі. Пробуються ПО ЧЕРЗІ, до першого успіху.
#
# Діагностика 06.08.2026: збої r.jina.ai — це ПЛАВАЮЧИЙ ліміт анонімного
# спільного пулу (той самий URL за хвилину віддав 403, потім 200), а не
# постійна блокада. Тому головне тут — повтор, а вже потім запасні шлюзи.
# Перевірено наживо: інші безкоштовні публічні проксі (allorigins, codetabs)
# самі не достукуються до ukr.net (521/522) і бувають повільні — їм даємо
# короткий таймаут, щоб мертвий шлюз не з'їдав хвилини запуску.
#
# (шаблон, чи_кодувати_url_у_query, таймаут_секунд)
READER_PROXIES = [
    ("https://r.jina.ai/{url}", False, 60),
    # Перевірено 06.08.2026: коли r.jina.ai віддавав 403, саме allorigins
    # витягнув повну стрічку (211 КБ). Але він на межі за швидкістю — інколи
    # не встигає за 12 с, тому 20: цей шлях і так вмикається лише коли
    # основний шлюз уже впав.
    ("https://api.allorigins.win/raw?url={url}", True, 20),
    ("https://api.codetabs.com/v1/proxy?quest={url}", True, 10),
]
# Скільки разів пробувати КОЖЕН шлюз (перша спроба + повтори) — саме це
# закриває плаваючий 403, бо він минає за секунди.
READER_PROXY_ATTEMPTS = int(os.environ.get("READER_PROXY_ATTEMPTS", "2"))
# Необов'язковий ключ jina.ai: дає власну квоту замість спільного анонімного
# пулу й прибирає причину плаваючих 403. Безкоштовний на jina.ai/reader.
JINA_API_KEY = os.environ.get("JINA_API_KEY", "")
READER_PROXY = READER_PROXIES[0][0].replace("{url}", "")  # сумісність
USER_AGENT = (
    "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 "
    "(KHTML, like Gecko) Chrome/126.0 Safari/537.36"
)
HTTP_TIMEOUT = 25

# --- Розклад (київський час) ---
# День: 06:00–00:59, ніч: 01:00–05:59
NIGHT_START_HOUR = 1
NIGHT_END_HOUR = 6

# Мінімальні інтервали між постами, хвилин — ЦЕ і є справжній регулятор темпу.
# Кожен інтервал — діапазон (від, до): фактичне число перемішується у ньому
# окремо для кожного вікна очікування (main.allowed_to_post), тож пости йдуть
# не рівним кроком, а органічно варіюються в межах діапазону.
#
# КРИТИЧНО: верхня межа діапазону МАЄ БУТИ МЕНШОЮ за крок запусків
# (cron-job.org, зараз ~7-8 хв) — інакше перша ж перевірка після посту часто
# не встигає (число з діапазону випадає БІЛЬШИМ за крок), і реальний
# інтервал стрибає одразу до наступного кроку (напр. 30 хв замість задуманих
# 10-20). NIGHT_* — виняток: там діапазони й так набагато ширші за крок,
# тому квантування на кроці запусків не помітне.
DAY_INTERVAL_HOT = (5, 10)       # вдень, якщо новина гаряча
DAY_INTERVAL_NORMAL = (10, 20)   # вдень, звичайна новина
NIGHT_INTERVAL_HOT = (60, 85)    # вночі, гаряча
NIGHT_INTERVAL_NORMAL = (90, 130)  # вночі, звичайна

# "Гаряча" новина = стільки або більше пов'язаних публікацій на Укрнеті
HOT_THRESHOLD = 25
# Додатковий пост за один запуск — лише для дуже термінових новин (було 100,
# тепер ближче до "гарячої": частіше спрацьовує, дає більше постів за день)
SECOND_POST_THRESHOLD = 55
# Пауза між постами в межах одного запуску: випадкова, хвилин (від-до).
# 06.08.2026 звужено 2-4 → 1-2. Ризик, про який попереджав попередній
# коментар, реалізувався: job "post" розросталась до 9-22 хв при тригерах
# кожні 5 хв, черга скасовувала 68% запусків. Сон УСЕРЕДИНІ запуску — найгірше
# місце для паузи: він тримає воркер і слот concurrency, нічого не роблячи.
# Рознесення постів у часі й так забезпечує сам крок тригерів (~5 хв).
POSTS_GAP_MINUTES = (1, 2)
# "Живий" розклад: випадкова затримка перед постом, щоб час не був рівно :00/:30
# 06.08.2026: 2 хв → 30 с. Тригери й так приходять некругло (кожні ~5 хв, а не
# на :00/:30), тож великий джитер більше не потрібен для «органічності» —
# він лише подовжував запуск.
JITTER_MAX_SECONDS = 30

# Підпис каналу в кінці поста. Ребрендинг 2026-08-02 (два етапи того ж дня):
# спершу змінилась лише видима назва, потім — і технічний @username
# (@News_Ukraine_world_war → @Suputnyk_news). Старий username ВІДКЛЮЧЕНО:
# getChat на нього повертає ok=false, тому всі посилання t.me/<старий>/<id>
# в опублікованих раніше постах і на SEO-дзеркалі — биті, це свідома ціна
# ребрендингу. Числовий chat_id (-1004460776464) не змінюється ніколи —
# якщо username мінятимуть ще раз, надійніше тримати в секреті саме його.
#
# CHANNEL_NAME — це те, як канал НАЗИВАЮТЬ (бренд), а не його username:
# заголовок каналу "Супутник — новини України та світу", у підписі поста
# лишаємо коротке "Супутник", бо шаблон уже додає тире:
# "📌 Супутник — підписатися". Довга форма дала б два тире поспіль.
CHANNEL_NAME = "Супутник"
CHANNEL_LINK = "https://t.me/Suputnyk_news"

# Вечірній дайджест "Головне за день"
DIGEST_HOUR = 21          # київська година публікації
DIGEST_MIN_ITEMS = 5      # мінімум постів за день, щоб дайджест мав сенс
DIGEST_MAX_LINES = 8

# Ранковий дайджест-картка (курси, день війни, пам'ятні дні)
MORNING_HOUR = 7
# Гороскоп на день
HOROSCOPE_HOUR = 9

# Альбом із кількох фото — для великих подій
ALBUM_THRESHOLD = 50      # від скількох публікацій шукати кілька фото
ALBUM_MAX_PHOTOS = 3
ALBUM_SOURCE_TRIES = 10   # у скількох статей кластера шукати фото для альбому
# У скількох джерелах кластера шукати пряме відео (відео цінніше за фото)
VIDEO_SOURCE_TRIES = 4
# Скільки джерел кластера завантажувати з Укрнету (буфер над ALBUM_SOURCE_TRIES —
# частина статей відсіється як заглушки/биті фото)
SOURCE_FETCH_MAX = 12
# Скільки джерел кластера порівнювати за якістю опису (ukrnet.description_quality).
# 4 — компроміс: сторінки цих джерел усе одно завантажуються далі для пошуку
# фото (src_meta кешує), тож зайвих HTTP-запитів майже немає, а вибірки
# вистачає, щоб оминути одне-два видання з версткою чи заглушкою в описі.
DESCRIPTION_SOURCE_TRIES = 4
# Цільова частка постів з відео за день. Якщо фактична нижча — агент свідомо
# бере відео-сюжет з великих TG-каналів, навіть коли Укрнет має новини.
VIDEO_TARGET_SHARE = 0.15
VIDEO_QUOTA_MIN_POSTS = 4   # квота вмикається лише після стількох постів за день
# М'якші пороги для відео-сюжетів квоти (свіжі відео не встигають набрати перегляди)
VIDEO_TREND_MAX_AGE_HOURS = 6
VIDEO_TREND_MIN_VIEWS = 5_000
# Добірка коротких відео однієї теми (media group з відео, як у УС)
VIDEO_ALBUM_MAX = 6       # максимум відео в добірці (ліміт Telegram — 10)
VIDEO_ALBUM_MIN = 2       # від скількох відео постити добіркою
# Мінімум пов'язаних публікацій, щоб новина взагалі розглядалась
MIN_RELATED = 2
# Не постити новини, старші за стільки годин
MAX_AGE_HOURS = 3
# Максимум постів за один запуск (лише якщо кандидати досить гарячі,
# SECOND_POST_THRESHOLD; кожен додатковий — з паузою POSTS_GAP_MINUTES)
MAX_POSTS_DAY = 3
MAX_POSTS_NIGHT = 2

# --- Тренди з великих Telegram-каналів (резервне джерело новин) ---
# Використовуються, лише коли Укрнет не дав гідних кандидатів.
TREND_CHANNELS = [
    "truexanewsua",   # Труха Україна
    "lachentyt",      # Лачен пише
    "insiderUKR",     # Інсайдер UA
    "operativnoZSU",  # Оперативний ЗСУ
    "suspilnenews",   # Суспільне Новини
    "ukrpravda_news", # Українська правда
    "unian",          # УНІАН
    "uanewsstream",   # UA.NEWS
]
TREND_MIN_VIEWS = 30_000    # мінімум переглядів, щоб пост вважався "гарячим"
TREND_MAX_AGE_HOURS = 3     # не старіші за стільки годин
TREND_MIN_TEXT = 80         # мінімальна довжина тексту (відсіює фото без контексту)
# Скільки вірусних (не про Україну/війну) трендів дозволено постити за день —
# для живості каналу, понад звичайні новини. Далі такі кандидати пропускаються.
VIRAL_QUOTA_MAX = 3

# Пошук фото/відео "тієї самої події" в каналах-конкурентах (build_post, крок
# "власного фото немає") — ширше вікно за TREND_MAX_AGE_HOURS: новина може бути
# ретроспективною аналітикою про подію кількаденної давнини (напр. огляд BBC про
# наслідки удару, опублікований через кілька днів після самого удару), тоді як
# канал-конкурент фотографував саму подію одразу. tgtrends.fetch_channel_history
# гортає ?before= сторінки t.me/s/ лише якщо справді потрібно глибше.
RETRO_MEDIA_MAX_AGE_HOURS = 72

# --- Консенсус каналів-гігантів: синхронна новина = термінова важлива подія ---
# Якщо та сама новина є одночасно в кількох із цих каналів — постимо невідкладно.
CONSENSUS_CHANNELS = ["truexanewsua", "u_now", "oko_ua"]  # Труха, УС, Всевидяще ОКО
CONSENSUS_MIN = 2           # мінімум каналів з однаковою новиною (з 3)
CONSENSUS_AGE_MIN = 20      # вікно синхронності, хвилин

# --- Терміновий алерт: обстріл/повітряна загроза Києва або по всій Україні ---
# Обов'язково й невідкладно, без жодних лімітів на кількість постів (лише не дубль).
KYIV_ALERT_AGE_MIN = 30     # свіжість поста-алерту, хвилин
ALERT_ALLCLEAR_MAX_AGE_MIN = 240  # доки чекаємо "відбій" для дописування в той самий пост

# --- Статистика (newsbot/stats.py) ---
STATS_STATE_FILE = ROOT / "stats_state.json"
STATS_REPORT_HOUR = 21  # київська година надсилання щоденного зведення

# --- Стан ---
STATE_FILE = ROOT / "state.json"
MAX_REMEMBERED_IDS = 500
MAX_REMEMBERED_TITLES = 60
# Поріг схожості заголовків (Жаккар за словами), щоб вважати новину дублем
TITLE_SIMILARITY = 0.55
# Скільки останніх РЕАЛЬНИХ фото (не generic/logo-фолбек) пам'ятаємо для
# перевірки дублів за хешем — Укрнет мутує cluster_id новини, що розвивається,
# AI пише щоразу новий заголовок (текстовий Jaccard це не завжди ловить), але
# фото походить з того самого джерела-статті — лишається ідентичним.
MAX_REMEMBERED_IMAGE_HASHES = 80
# Макс. відстань Геммінга (з 64 біт) між перцептивними хешами, за якої
# вважаємо фото тим самим. Вивірено 15.08.2026 на 5 реальних новинних фото
# (10 пар) та їхніх трансформаціях:
#   те саме, перестиснене  0-1     те саме, кроп 5%   1-13
#   те саме, зменшене      0       те саме, кроп 10%  6-23
#   РІЗНІ фото             22-37
# 10 ловить перестиснення, масштаб і більшість кропів, лишаючи 12 біт
# запасу до найближчого "різного фото". Сильний кроп (>10%) цей метод
# принципово не бере — dHash не інваріантний до обрізання.
IMAGE_DHASH_MAX_DISTANCE = 10

# --- Пости ---
CAPTION_LIMIT = 950  # ліміт Telegram для підпису до фото — 1024
MIN_IMAGE_BYTES = 8_000  # менші картинки вважаємо битими/заглушками
MIN_IMAGE_WIDTH = 550    # відсіюємо неякісні фото (кадри з відео, прев'юшки)
MIN_IMAGE_HEIGHT = 320
IMAGE_SOURCE_TRIES = 10  # у скількох статей кластера шукати пристойне фото
                          # (перед тим, як здатись і генерувати AI-ілюстрацію)
# Макс. частка 3 домінантних кольорів: вище — це текстова заглушка видання,
# а не фото (вивірено на живих даних: заглушки ≥0.65, фото ≤0.51)
IMAGE_FLAT_TOP3 = 0.62
# Послаблений поріг для ДРУГОЇ спроби — коли за суворим не пройшло жодне фото
# з усіх джерел і альтернатива це пост зовсім без картинки.
# Розбір 15.08.2026 (новина про удар по Миколаївщині): справжнє фото niknews
# дало 0.66 — воно лише трохи перетнуло поріг і відкинулось разом зі
# скриншотом (0.76), а пост вийшов порожнім. За добу так втрачалась половина
# картинок (8 фото на 16 постів). Посереднє фото краще за жодного, тож тут
# пропускаємо все, крім явних заглушок (вони зазвичай 0.85+).
IMAGE_FLAT_TOP3_RELAXED = 0.80
MIN_VIDEO_BYTES = 100_000
MAX_VIDEO_BYTES = 45_000_000  # ліміт завантаження для ботів Telegram — 50 МБ
